# React 19 + Radix UI + @visx/visx peer-dep matrix isn't fully aligned at the
# time of this lockfile. Skipping strict peer-dep resolution so `npm ci` and
# `npm install` succeed against the committed package-lock.json. The runtime
# behaviour is unaffected — peer-dep warnings remain visible in install output.
legacy-peer-deps=true
