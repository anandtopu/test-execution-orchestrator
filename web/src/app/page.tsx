import { RunDetailScreen } from '@/components/teo/RunDetail';

// Home = the marquee "Live run" screen (shard Gantt + side panels).
export default function HomePage() {
  return <RunDetailScreen />;
}
