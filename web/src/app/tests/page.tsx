import { Placeholder } from '@/components/teo/Placeholder';

// Stubbed in the design ("not designed in this prototype").
export default function TestsPage() {
  return <Placeholder title="Tests" subtitle="Per-test history, ownership, duration trends." />;
}
