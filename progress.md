# TEO — Implementation Progress

**Last updated:** 2026-05-20 (⚠ **backlog reconciliation** found **12 items marked ✅ here that were not wired in code** — see [Backlog reconciliation](#backlog-reconciliation--verified-incomplete-items-2026-05-20) below. **Then fixed 6 of them: #1 `teo replay`, #2 S3 plan archive, #3 Go AST fingerprint, #4 Python AST fingerprint, #5 log-tail viewer, #6 OIDC sign-in** (+ added `teo discover`, migration 006). **All functional gaps closed; 6 polish/test-debt items remain** (#7–#12). Prior 2026-05-05 entry: Gap #5 GraphQL authz gate landed; `internal/nats` unit-tested.)
**Build status:** ✅ `go build ./...` clean · ✅ 26 Go unit packages green (incl. `cmd/predictor` healthz-proxy + no-args smoke) · ✅ 47 integration tests under `-tags=integration` (testcontainers Postgres for GraphQL + run-intake REST + Runs gRPC bufconn + run-export + cost resolvers + **predictor Heuristic loadStats**; testcontainers MinIO for the logstore S3 round-trip; runs in CI — the new `internal/predictor/heuristic_integration_test.go` compiles via `go vet -tags=integration` but was not run locally, Docker unavailable) · 6 frontend test files (Vitest, runs in CI) · ✅ 14 predictor-ml pytest cases green (`services/predictor-ml/tests/`, FastAPI TestClient + feature extraction)
**Reading order:** [PRD](PRD.md) → [Architecture overview](docs/architecture/overview.md) → [ADR-0012 scope](docs/adr/0012-mvp-scope-cut.md) → this file.

> **▶ Resume here next session.** ⚠ **The "functionally release-ready / no known blockers" status was overstated.** A 2026-05-20 code audit found **12 items marked ✅ that were not implemented** (see [Backlog reconciliation](#backlog-reconciliation--verified-incomplete-items-2026-05-20)); five were functional ACs. **6 are now fixed** (#1 `teo replay`, #2 S3 plan archive, #3 Go AST fingerprint, #4 Python AST fingerprint, #5 log-tail viewer, #6 OIDC sign-in) — **every functional gap is closed; 6 polish/test-debt items remain** (#7–#12). The infrastructure that *is* done remains real — `go build ./...` clean, 25 unit packages + 38 integration tests green, goreleaser verified end-to-end (`goreleaser release --snapshot --clean --skip=sign,sbom,announce` against v2.15.4 produces all 7 services × linux/darwin amd64/arm64 + windows for `teo`, plus archives + checksums in ~90s), protoc-gen-go wiring landed, `docs/release-notes/v1.0.0.md` drafted. **But triage the open items into v1.0-must-fix vs v1.1-defer before tagging.**
>
> After the open items are triaged/closed, two manual steps remain to ship — both require human authorization:
> 1. **Run the restore drill** against staging per [`docs/operations/restore-drill.md`](docs/operations/restore-drill.md) and record the outcome in the runbook's history log.
> 2. **Tag and push:** `git tag v1.0.0 && git push --tags` — `release.yml` then runs goreleaser (binaries + cosign + Syft SBOMs) and chart-releaser (Helm chart published to gh-pages). After GitHub creates the Release, paste `docs/release-notes/v1.0.0.md` into the description.
>
> At tag time, also roll the `[Unreleased]` block in `CHANGELOG.md` under `## [v1.0.0] — <YYYY-MM-DD>`.

This is the single source of truth for implementation status. Every entry links to the code or doc it tracks. Status legend:

- ✅ **Done** — code lands, tests pass, doc reflects reality
- 🟡 **Scaffolded** — wire-correct skeleton in place, expected behavior covered, but a follow-up is required (called out per row)
- ⏳ **Pending** — agreed in scope, not yet started
- 📦 **Deferred** — out of scope for v1.0 per ADR-0012 (revised)

---

## Backlog reconciliation — verified incomplete items (2026-05-20)

A code-level audit of every story/task in `docs/backlog/{stories,tasks}.md` against the source tree found the items below. Each was **marked ✅ elsewhere in this file but is not wired in code** — verified by inspecting the source, not the docs. (The older `stories.md`, last touched 2026-04-30, still honestly flags most of these as ⏳/🟡; `progress.md` had drifted ahead of reality.)

| # | Item | Story / Task | Status | Evidence (verified in code) |
|---|---|---|---|---|
| 1 | `teo replay <run_id>` CLI | S-05-04 AC2 / T-05-04-02 | ✅ **done** (2026-05-20) | [`cmd/teo/replay.go`](cmd/teo/replay.go) reads `runs.meta.computed_plan`, re-runs `scheduler.Replay` with `scheduler.DefaultConstraints()`, and reports determinism (exit 1 on mismatch; `--json` flag). Unit-tested in [`internal/scheduler/replay_test.go`](internal/scheduler/replay_test.go) |
| 2 | Assignment plan persisted to **S3** `runs/<id>/plan.json` | S-05-04 AC1 / T-05-04-01 | ✅ **done** (2026-05-20) | Run Manager archives the computed plan to `runs/<id>/plan.json` (`Manager.PlanStore` / `runmanager.PlanObjectKey`, best-effort, gated on `TEO_S3_BUCKET`). `logstore.S3.Download` + `teo replay --from-s3` read it back; the Postgres copy is retained. Unit-tested (key/body, no-store no-op, error-swallow) + MinIO download round-trip in the integration suite |
| 3 | Go AST-signature fingerprint | S-14-01 AC4 / T-14-01-04 | ✅ **done** (2026-05-20) | [`pkg/adapter/gotest/astsig.go`](pkg/adapter/gotest/astsig.go) parses each package's test files via `go/ast` and hashes the normalized function body; attached to `TestEntry.ASTSignature`. Stable across reformatting/comments, changes with logic. Unit-tested incl. end-to-end via a temp module |
| 4 | Python AST-signature fingerprint | S-06-01 / T-06-01-03 | ✅ **done** (2026-05-20; param-lookup keying hardened 2026-06-10, no behavior change) | [`pkg/adapter/pytest/astsig.go`](pkg/adapter/pytest/astsig.go) runs an embedded `ast`-module helper to hash each test's body (module funcs + `Test*` class methods). `Discover` looks up the signature by `stripParams(entry.Name)` (via the pure `attachSignatures` helper), so the bare-qualname keying is correct by construction regardless of any `[param]` suffix on `Name`. **No observable change today** — `parseCollect` already pre-strips the suffix into `ParamsHash`, so parametrized variants already resolved; the change decouples the lookup from that implicit invariant and pins it with `TestAttachSignatures`. Param-lookup keying is further covered by [`pkg/adapter/pytest/astsig_param_test.go`](pkg/adapter/pytest/astsig_param_test.go) (`TestParametrizedVariantsInheritBaseSignature` — both `[case1]`/`[case2]` variants inherit the base sig; `TestParametrizedClassMethodLookupKey`; `TestNonParametrizedAttachUnchanged` regression guard; `TestStripParamsBareAndClass` helper contract; Python-gated end-to-end `TestParametrizedVariantsInheritBaseSignaturePython`). Go adapter verified unaffected (`go test -list` only emits bare `Test*`/`Benchmark*`/`Example*` names) and pinned by [`pkg/adapter/gotest/discover_noparam_test.go`](pkg/adapter/gotest/discover_noparam_test.go) (`TestGoDiscoverNoParamSuffix` — a table-driven test discovers only the bare `TestTable` name, no `[`/`/` suffix, signature attaches via `sigs[pkg+"::"+name]`). Unit-tested end-to-end against a real Python interpreter |
| 5 | UI log-tail viewer (S3 presigned URL, paginated) | S-09-03 AC1 / T-09-03-03 | ✅ **done** (2026-05-20) | `logstore.Presigner` + `S3.Presign`; API `GET /api/v1/runs/{id}/tests/{execId}/log` (ownership-checked, 501 when S3 unconfigured); Next BFF `/api/logs` proxies the presigned URL's tail via HTTP Range; `LogTail` component + `runs/[id]/tests/[execId]` page. Tested both sides. *Remaining:* a per-test list linking into the viewer (broader S-09-03) |
| 6 | UI OIDC sign-in flow + JWT refresh | S-03-02 AC1 / T-03-02-03 | ✅ **done** (2026-05-20; frontend wired 2026-06-10) | New dependency-free [`internal/oidc`](internal/oidc) (discovery, code exchange, JWKS RS256 verify); API `/auth/{login,callback,logout,session,refresh}` issue an HS256 JWT in an httpOnly `teo_session` cookie; `/login` page + `SessionNav`; `auth.oidc.*` Helm values wired into the api deployment. **Frontend now uses it** (`oidc-frontend`): new `web/src/middleware.ts` soft-gates the UI on the `teo_session` cookie behind opt-in `NEXT_PUBLIC_REQUIRE_AUTH=1` (default off so an OIDC-disabled backend stays usable; allow-lists `/login`, `/auth/*`, BFF `/api/*`, static); `SessionNav` is mounted in the Shell topbar (replacing the hardcoded avatar) and proactively POSTs `/auth/refresh` on a fixed interval (`NEXT_PUBLIC_SESSION_REFRESH_MS`, ~10m), flipping to Sign-in on a non-OK refresh. Unit-tested |
| 7 | UI predicted-vs-observed calibration overlay | S-12-04 AC2 / T-12-04-01 | ✅ **done** (2026-06-10) | The `/` home screen is now a data-fetching Server Component (`web/src/app/page.tsx`): it resolves the most-recent run via `RunsQuery` + `RunByIdQuery`, adapts the GraphQL Run/Shard shape through the pure `adaptRun`/`adaptStatus` adapter in [`web/src/lib/teo-adapt.ts`](web/src/lib/teo-adapt.ts) (status-vocabulary mapping succeeded→pass / lost→fail / preempted→preempt, ms→seconds, run-level rollups + a real predicted-vs-actual p50/p95 delta distribution), and feeds the teo/ `RunDetailScreen` real props (design unchanged) — no longer `TEO_DATA`. The live 2s poll runs via the `LiveRunDetail` client wrapper, which re-fetches `/api/graphql/run` and re-adapts while `isLive()`. `RunByIdQuery` gained additive `predictorMae`/`predictorRho`/`modelVersion` (run) + `predictionConfidence`/`modelVersion` (shard); the GraphQL backend exposes them (`internal/api/graphql.go` `runPredictorFieldResolve` + shard `mapResolve`, resolving to null until the sibling `graphql-schema-fields` migration adds `teo.shards.prediction_confidence`/`model_version`). `LiveRunShards`/`GanttBar` now draw the predicted band + actual bar side-by-side (a genuine calibration view) and surface `· conf NN%`, instead of the old `actual ?? predicted` single bar. Graceful degradation: when confidence/modelVersion/MAE are null the overlay renders an em-dash, not a crash. **Review-fix sweep (2026-06-10):** home route wrapped in try/catch so a backend connection-reject degrades to the empty state (not a crash); predictor MAE now converted ms→s in `buildRunPredictor`; live `elapsed` resyncs to the polled value and freezes on terminal status; `STATUS_MAP` + `isLive` reconciled to the backend run-status vocab (`succeeded`/`canceled`/`cancelled`/`lost`); `queryRunPredictor` memoized per-request (`cachedRunPredictor`) so the nested + 3 flat predictor resolvers share one computation (8 DB round-trips → 2 per poll); `queryShards` comment corrected (per-shard confidence/model_version resolve null until the migration wires the SELECT — not automatic); new `adaptRun`/`adaptStatus`/`buildRunPredictor` unit tests + a `TestSDLAgreesWithProgrammaticSchema` parity test. |
| 8 | Mermaid run-history viz in auto-quarantine issue body | S-15-02 AC1 / T-15-02-02 | ✅ **done** (2026-06-10) | `buildIssueBody` now appends a `## Recent run history` section rendered by the pure, side-effect-free helper [`renderRunHistoryMermaid`](internal/quarantine/mermaid.go) — a GitHub-rendered ```mermaid `xychart-beta` bar chart of recent pass/fail outcomes (1=pass, 0=fail), oldest→newest, capped at `maxHistoryPoints=30` (keeps the most recent N). `skipped`/`interrupted` runs are classified neutral and filtered out of the strictly pass/fail series with a caption noting the omitted count (`classifyOutcome`, returning an `outcomeClass` enum — `outcomePass`/`outcomeFail`/`outcomeNeutral`); zero-history (including all-neutral-after-filtering) degrades to a plain `_No recent run history available._` line (no fence, since Mermaid errors on an empty x-axis). The daemon's new `Daemon.recentOutcomes` selects one row **per run** (`DISTINCT ON (shard_id)` final-attempt, so a retried flaky test charts as a single bar, not one bar per attempt), newest-run-first then reversed to oldest→newest; it over-fetches (`maxHistoryPoints*3`) so the render-side pass/fail cap — not the DB row count — bounds the chart, and uses `te_test_started_idx` directly (the vestigial `JOIN teo.shards` was dropped since `test_executions.shard_id` is `NOT NULL` FK). Best-effort (slog.Warn + empty slice on error, never blocks issue creation). `internal/github`'s issues client marshals `IssueRequest.Body` verbatim — the fenced block passes through unmodified (no production change). Unit-tested in [`internal/quarantine/mermaid_test.go`](internal/quarantine/mermaid_test.go) (helper edge cases: nil/empty, all-neutral→empty-no-fence, mixed/all-pass/all-fail bar series + 1-based x-axis, caption count + pluralization, >30 truncation keeping the TAIL, unknown-outcome→fail, `classifyOutcome` table, determinism) plus `buildIssueBody` section/fence presence + empty-degrade in `quarantine_test.go`. Integration test [`internal/quarantine/mermaid_integration_test.go`](internal/quarantine/mermaid_integration_test.go) (`//go:build integration`, requires Docker) seeds Postgres (incl. a retried attempt + a skipped run) and asserts `recentOutcomes` collapses retries per-run and the e2e issue body's Mermaid block — **compiles under `go vet -tags=integration` but NOT executed here (no Docker)**. **Test-coverage sweep (2026-06-10):** added explicit spec-fixture unit tests in `mermaid_test.go` (mixed `passed,failed,passed`→`bar [1, 0, 1]` no-neutral ordering; no-NaN/no-empty-`bar []` on skipped/interrupted mix; 50-outcome truncation keeping the most-recent 30; a byte-exact golden on the full rendered block for determinism); a `github` body pass-through test [`TestIssuesCreatePreservesMermaidFence`](internal/github/issues_test.go) POSTing a ```mermaid block through `IssuesClient.Create` against an httptest server and asserting the raw wire body preserves the fence and bar series verbatim (round-trips byte-for-byte); and a full-sweep e2e [`TestQuarantineSweepE2E`](internal/quarantine/mermaid_integration_test.go) (`//go:build integration`) that seeds a Wilson-confirmed flake (`wilson_lower=0.10`, `sample_size=25`, `auto_quarantine_enabled=true`) + mixed executions, injects a capturing fake `IssueOpener`, runs `Daemon.Run`, and asserts `tests.status='quarantined'`, the captured body's `bar [1, 0, 1]` matches seeded oldest→newest order, and `flake_records.github_issue_number` persisted — **compiles under `go vet -tags=integration`, NOT executed (no Docker)**. |
| 9 | Failure-cluster backfill job | T-07-02-03 | ✅ **done** (2026-06-10) | [`internal/resultpipeline/backfill.go`](internal/resultpipeline/backfill.go) `Backfiller.Run` scans failed/errored/timed_out `test_executions` with a non-empty `otel_trace_id` but NULL `failure_cluster_id` (`PGExecSource.PendingFailures`, covered by `te_outcome_idx`), fetches the stack+message from ClickHouse `span_events` (`CHStackSource.StackFor`, `status_code=2` error span, `argMax` by `end_time`, message falls back to `status_message` via an `if(empty(...))` check mirroring the live `firstNonEmpty`), reuses the **exact live-ingest fingerprint** (`FingerprintStack`), and resolves the cluster id via the **count-free** `*Cluster.ClusterFor` — **NOT** the occurrence-incrementing `UpsertCluster` — because the target failures were already counted once at OTLP ingest, so re-counting here would double-count and corrupt cluster ranking; backfill is a pure back-link. It then links via `AssignCluster` (`UPDATE … WHERE failure_cluster_id IS NULL`, idempotent on re-run). Per-trace caches dedupe `StackFor`/`ClusterFor` so N executions on one trace cost one CH query + one resolve; `PendingFailures` is keyset-paginated (`ORDER BY te.id LIMIT 5000`) so an all-history scan never materializes unbounded. This closes the gap that the OTLP path upserts a cluster but never sets `test_executions.failure_cluster_id` (only the worker gRPC path did). Seams (`ExecSource`/`StackSource`/`ClusterResolver`) make `Run` unit-testable with no DB; `--dry-run` fingerprints without mutating; `--since` bounds the scan (0 = all). Wired as `result-pipeline backfill-clusters [--since <dur>] [--dry-run]` (mirrors `owner-digest`/`flake-recompute`; first cron needing both Postgres + ClickHouse DSNs) and as a default-off Helm CronJob (`backfillClusters.{enabled,schedule,since}`, injects both `postgres-creds` + `clickhouse-creds`). Unit tests cover every Run branch (`backfill_test.go`, stub seams, no DB: happy/dry-run/no-stack/no-fingerprint/StackFor-err/ClusterFor-err/AssignCluster-err/fatal-scan/per-trace-dedupe). An `//go:build integration` `internal/testpg` end-to-end test is the remaining follow-up (needs Docker, unavailable here). |
| 10 | ClickHouse 1M-row load-test harness + p99 numbers | S-02-03 / T-02-03-02 | ✅ **done** (2026-06-10) | New [`internal/testch`](internal/testch/testch.go) testcontainers harness (mirrors `internal/testpg`/`internal/testminio`): spins up `clickhouse-server:24.8-alpine`, gates readiness on an HTTP `/ping` of the 8123 port (no `time.Sleep`), runs `migrate.Up(ClickHouse, …)` against `repoMigrationsDir()`, and returns a native `OpenClickHouseConn` driver.Conn + DSN + cleanup. [`otlp_loadtest_integration_test.go`](internal/resultpipeline/otlp_loadtest_integration_test.go) (`//go:build integration`) drives the **exact production write path** `OTLPReceiver.writeSpans` (`otlp.go:132`, native `PrepareBatch`): inserts ~1M rows (`loadTestRowCount`: 1M default, 10k under `-short`, `TEO_LOADTEST_ROWS` override) in `TEO_LOADTEST_BATCH`-sized batches (5k default), builds each batch's `synthRows` **before** the timer so generation never pollutes the numbers, times only `writeSpans`, computes throughput + p50/p95/p99 (`percentile`, nearest-rank) and asserts `count()` equals the total (plain `MergeTree`, exact). Pure logic (`percentile`, `synthRows`, `loadTestRowCount`/`loadTestBatchSize`) lives in no-build-tag [`loadmetrics.go`](internal/resultpipeline/loadmetrics.go) so it's unit-testable without Docker. Run command + a not-yet-run results table in [`docs/operations/clickhouse-load-test.md`](docs/operations/clickhouse-load-test.md). Compiles + `go vet -tags=integration` clean; **not executed here** (Docker unavailable) — p99 table left as `TODO` rather than fabricated, to be filled from the first CI testcontainers run. |
| 11 | Run-list virtual scroll for 100+ rows | S-09-01 AC / T-09-01-03 | ✅ **done** (2026-06-10) | `web/src/app/runs/page.tsx` now fetches `runs(first: 200)` (backend `clampFirst` cap; no backend change) and renders via the new `'use client'` [`RunsTable`](web/src/components/RunsTable.tsx). RunsTable does dependency-free fixed-row-height windowing (no `@tanstack/react-virtual` — repo avoids extra UI deps, cf. the cost dashboard's CSS bars): it renders only the rows whose index falls in the visible scroll window + overscan, deriving `visibleCount` from `height`/`rowHeight`/`overscan` props (deterministic under jsdom, which has no layout engine) and preserving the full scrollbar extent with two `aria-hidden` spacer rows. Page stays a Server Component passing server-fetched rows down as props (mirrors `LiveRunShards` initial-data pattern); existing columns/styling unchanged. |
| 12 | Leader-election 2-replica chaos/integration test | S-04-02 / T-04-02-03 | ✅ **done** (2026-06-10) | [`internal/runmanager/leader_election_integration_test.go`](internal/runmanager/leader_election_integration_test.go) (`//go:build integration`, `internal/testpg`). `TestLeaderElection_LockHeldBlocksSecondReplica` is the deterministic core: replica A holds the per-run `pg_try_advisory_xact_lock` in an open tx (same `runIDLockKey` the Manager uses) → replica B's `tryHandle` is a no-op (run stays `pending`, 0 shards) → A's tx rolls back (lease ends) → B takes over and plans. `TestLeaderElection_ConcurrentReplicasConvergeWithoutDuplicateShards` is the chaos test: 4 replicas (own Managers, shared pool, barrier-synced) hammer `reconcileOnce` against one pending run; it converges to `running` exactly once with no duplicate `(run_id,index)` shards. The design is race-free because the lock and the `pending→planning` UPDATE commit atomically, so a competitor never sees `status='pending'` with the lock free (MVCC). **Executed against real Postgres via testcontainers — both PASS** (unlike the Docker-gated tests above, Docker was available this run) |

**Not gaps (deferred by an explicit decision — leave as-is):**
- WebSocket subscriptions (S-09-02 AC3 / T-09-02-02) — superseded by 2 s polling, which meets NFR-PERF-107's 3 s SLO; deferred to v1.1.
- Jest AST-signature fingerprint (S-14-02 AC3) — explicitly deferred to v1.5; path + name only at v1.0.

**Release impact:** items **1, 2, 3, 4, 5, 6 are now resolved** (2026-05-20) — every functional gap from the audit is closed. The remaining polish/test-debt items **7, 8, 9, 10, 11, 12 are all now done** (2026-06-10) — **the entire 12-item reconciliation list is closed.** #12 (leader-election) was additionally executed against real Postgres, not just compile-verified.

> **AST-fingerprint wiring (#3/#4).** The test fingerprint is now `path::name::paramsHash::astSig`. `model.TestEntry.ASTSignature` (+ `nats.DispatchTest`) carries the signature through both worker claim paths (Postgres SKIP-LOCKED and NATS dispatch); migration 006 adds `teo.tests.ast_signature` (indexed for future move/rename linking). A new **`teo discover --runner <r> [dir]`** CLI runs the adapter and emits a manifest JSON with signatures populated — the production producer, which also closes the previously-missing **S-06-01 AC1**. Jest AST sig stays deferred to v1.5 per S-14-02 AC3 (empty signature). Tradeoff: a test whose body changes gets a new fingerprint (fresh flake history) — intended, and documented in `internal/worker/worker.go`.

---

## Epic-level dashboard

> **All reconciliation acceptance criteria are now closed** (2026-06-10) — #7 (E-12), #8 (E-15), #9 (E-07), #10 (E-02), #11 (E-09) and #12 (E-04) all landed. Any remaining 🟡 on the rows below reflects scope **deferred by decision** (e.g. E-09 WebSocket subscriptions → v1.1, E-12 per-test ML confidence out of scope), not an open audit item. E-02/E-04/E-07 keep ✅ (#12 leader-election test executed green against real Postgres for E-04; #10 ClickHouse load harness landed for E-02 with p99 awaiting a CI run).

| ID | Epic | Status | Notes |
|---|---|---|---|
| **E-01** | Foundation: monorepo, CI, base services | ✅ | `go.mod`, `Makefile`, `.golangci.yml`, `.github/workflows/ci.yml`, all hygiene files. 7 service stubs compile. |
| **E-02** | Postgres + ClickHouse schema + migrations | ✅ | Initial migration files for both stores. `internal/migrate` runner + `teo migrate` CLI subcommand. |
| **E-03** | API gateway + auth + audit log | ✅ | chi-based REST (`/api/v1/runs` POST/GET/cancel), JWT (HS256) issuer, argon2id-hashed API keys with 30s revocation cache, audit middleware. RFC 7807 errors. |
| **E-04** | Run Manager state machine + leader election | ✅ | 8-state machine with legality table. Per-run `pg_try_advisory_xact_lock` (ADR-0013). Budget enforcement loop. |
| **E-05** | Scheduler (LPT) + heuristic predictor | ✅ | Pure-function `PlanFunc`, exclusivity tags, quarantine lane, deterministic tie-break, brute-force property test verifies makespan ≤ 4/3 × OPT over 30 random instances. |
| **E-06** | Worker agent + pytest adapter + redactor | ✅ | Worker claims via Postgres SKIP-LOCKED; pytest adapter parses `--collect-only` + `--json-report`; redactor scrubs AWS keys, JWTs, GitHub PATs, generic high-entropy patterns. **Per-test log capture** (FR-404) uploads redacted stdout/stderr to S3 through `internal/logstore` (transfermanager-driven, auto-multipart >16MB); the resulting object key is persisted into `test_executions.log_object_key`. |
| **E-07** | Result pipeline (OTLP + failure clustering) | ✅ | OTLP gRPC receiver implements `collectorpb.TraceServiceServer` on `:4317`, writes to ClickHouse `teo.span_events`, routes ERROR-status spans into `failure_clusters`. Python-aware traceback fingerprinting + generic fallback. **JUnit + OTLP run-exports** wired at `GET /api/v1/runs/{id}/export?format={junit\|otlp}` — JUnit reads `test_executions`+`failure_clusters` from Postgres (one `<testsuite>` per shard), OTLP reads `teo.span_events` via the `SpanQuerier` interface and emits binary protobuf (or `?as=json` for `protojson`). |
| **E-08** | Flake detection (Wilson interval) | ✅ | `WilsonInterval` formula with textbook tests. Nightly job (`internal/flake/job.go`) classifies flaky / broken / insufficient. |
| **E-09** | Web UI (Next.js + GraphQL) | 🟡 | All four pages migrated to **GraphQL only** (zero `/api/v1/*` REST calls left in `web/src/app/`). Run-detail page uses a **2s-polling Client Component** (`LiveRunShards`) that auto-stops when status is terminal — meets NFR-PERF-107's 3-second SLO. **Rerun-failed button** wired with mutation proxy. Two Next API routes (`/api/graphql/run`, `/api/graphql/rerun`) keep the API key server-side. **6 frontend Vitest files** + **13 GraphQL integration tests** under `-tags=integration` (testcontainers-driven real-Postgres tests covering every resolver, the run-by-id query, the rerunFailed mutation, schema error envelope, and the full HTTP roundtrip). New observability fields (`Run.predictor`/`RunPredictor`, `Shard.deltaMs`, `FailureCluster.{x,y,r,category,stackFingerprint,affectedRuns}`, `FlakeRecord.{wilsonUpper,spark,status,durationMeanMs}`) now have **Go unit coverage for the pure helpers** (`graphql_layout_test.go`: `classifyClusterCategory`/`clampFloat`/`pearson`/`percentile`/`flakeStatusBadge`/`computeClusterLayout` + `encodeSparkline` (`TestSparklineEncoding`) and `TestClusterCoordsDeterministic` for the layout bounds/monotonicity contract) and **schema-shape guards** (`graphql_schema_test.go`: `RunPredictor`/`FailureCluster`/`FlakeRecord` field checks + `Shard.deltaMs`). Their backing SQL resolvers gained **integration-tier coverage** (`graphql_integration_test.go` seed extended with a 2nd cluster + 25-execution sparkline run; `TestQueryFailureClustersComputesLayout`/`TestQueryFlakesIncludesSparklineAndWilsonUpper`/`TestQueryRunPredictor`) and the **HTTP roundtrip** (`graphql_http_integration_test.go`) now selects the new FailureCluster/Flake/`predictor { mae }`/`shards { deltaMs }` fields end-to-end; `web/src/lib/queries.test.ts` guards the new field selections (Docker-gated tiers compile via `go vet -tags=integration`, not run where Docker is unavailable). WebSocket subscriptions remain deferred to v1.1 per strategy §3 (polling meets the freshness SLO). |
| **E-10** | GitHub App integration + Checks API | ✅ | Webhook receiver with HMAC SHA-256 verification, installation event handler, Check Runs + Issues REST clients. **`runmanager.RunObserver` interface** dispatches a snapshot to every registered observer after each successful state transition (post-commit, so observer latency can't hold the lock). **`github.CheckObserver`** creates the Check Run on the first transition out of `pending`, updates it on every in-flight transition with shard counts, and finalizes with conclusion + Markdown summary including the **top-3 failure clusters** for the run (S-10-03). Migration 003 adds `runs.github_check_run_id` + `runs.github_installation_id`. cmd/run-manager wires the observer when `TEO_GITHUB_TOKEN` is set; warns and skips otherwise. |
| **E-11** | Helm chart + observability + release pipeline | ✅ | Umbrella chart with API/Run Manager/result-pipeline Deployments, migrations pre-upgrade Job, three quarantine + digest + ML-train CronJobs, Karpenter NodePools. **Subcharts vendored** in `Chart.yaml` (CloudNativePG, ClickHouse Operator, NATS, MinIO, Dex) — each toggleable so operators can BYO managed services. **Five Grafana dashboards** as ConfigMaps (API latency, scheduler, run state machine, ClickHouse lag, NATS lag) labeled for sidecar discovery. **Six PrometheusRule alerts** with `runbook_url` annotations pointing at `docs/operations/runbooks/`. **`.goreleaser.yml`** for binary releases (linux/darwin amd64/arm64) with cosign keyless signing + Syft SBOMs; **release.yml** workflow runs goreleaser then publishes the chart to gh-pages via chart-releaser. **chart-testing config** (`.github/ct.yaml`) + upgraded CI helm job that templates the on/off subchart matrix. **Restore drill runbook** at `docs/operations/restore-drill.md` with reproducible 8-section procedure and history log. |
| **E-12** | ML predictor (Python, LightGBM) | 🟡 | FastAPI app, model registry with S3-backed per-repo TTL cache, training script with champion/challenger gating, **real ClickHouse query** for training data, synthetic fallback for dry-runs. Dockerfile. **Now wired end-to-end (FR-607):** Go `MLClient` + `Fallback` call `/v1/predict` from the Run Manager under `TEO_PREDICTOR_ML_URL` with heuristic auto-fallback; Python `_resolve_repo_id` (Postgres) + `_features_for` (ClickHouse history) exercise the real model path with genuine cold-start fallback; `cmd/predictor` is a health-proxy. UI calibration overlay (#7) landed 2026-06-10 — see Reconciliation row 7. |
| **E-13** | Karpenter + spot-aware scheduling + checkpointing | ✅ | IMDSv2 poller (`internal/spot/`) wired into Agent via the `SpotInterruptionSource` interface. **Draining state machine** in the Agent: atomic `draining` flag, current-shard tracking, `beginDrain()` marks the in-flight shard `preempted` and bumps `runs.preemption_count`. Both the Postgres-claim and NATS-handler paths early-return when draining; NATS naks back so a non-draining worker picks up the message. **Run Manager reschedule sweep** (`reschedulePreempted`, every 5s) finds preempted/lost shards, computes the set of unfinished tests (intended-via-round-robin minus already-recorded `test_executions`), and creates a fresh pending shard with that residue under `runs.meta.reshards[<new_shard_id>]`. Worker's `loadTestsForShard` consults that map first, falling back to round-robin. Migration 004 adds `shards.meta` for the `rescheduled_at` dedupe marker. Three integration tests cover the happy path (only-uncompleted), all-completed no-op, and idempotent-second-sweep dedupe. |
| **E-14** | Additional runner adapters | ✅ | `pkg/adapter/gotest` parses `go test -json` events; `pkg/adapter/jest` runs Jest with `--json --outputFile` and parses assertion results. **SPI contract documented at `docs/adapters/spi.md`** (interface, Discover/Execute semantics, fingerprint/redaction/OTel boundary, conformance checklist) with a copy-and-fill skeleton at `pkg/adapter/template/`. |
| **E-15** | Auto-quarantine workflow + GitHub Issue creation | 🟡 | CODEOWNERS parser + matcher, quarantine daemon transitions tests with broken-vs-flaky distinction. **GitHub Issues REST client** (`internal/github/issues.go`) wires Open/Comment/Patch via the same auth model as Check Runs. Re-quarantine dedupes via comment instead of duplicate issue. **SLA nudge sweeper** (`internal/quarantine/sla.go`) posts after `flake.sla_days` and is idempotent on `last_nudged_at`. **Un-quarantine proposer** (`internal/quarantine/unquarantine.go`) tracks K consecutive passes, posts a magic-link comment; `/api/v1/quarantine/restore?token=…` consumes it (single-use, token-gated, no auth required since the link is the auth). |
| **E-16** | Owner digest (weekly Slack/email) | ✅ | HTML + plain-text templates, per-author aggregation, SMTP sender (with mock-dialer tests), Slack webhook sender (httptest-verified), Multiplex fanout, opt-out enforcement (per-user via `users.digest_opt_out`, per-repo via `repos.meta.digest_opt_out`), `result-pipeline owner-digest` subcommand wired into the Helm CronJob, `teo digest dry-run --user=<email>` CLI for inspection. |

---

## Gap-closeout PR (post-E-16)

These were called out as the realistic gaps after the E-01..E-16 sweep, and are now resolved:

| # | Gap | Status | Code |
|---|---|---|---|
| 1 | OTLP gRPC ingest server | ✅ | [`internal/resultpipeline/otlp.go`](internal/resultpipeline/otlp.go), [`cmd/result-pipeline/main.go`](cmd/result-pipeline/main.go) |
| 2 | NATS dispatch + worker subscriber | ✅ | [`internal/nats/`](internal/nats), [`internal/worker/nats.go`](internal/worker/nats.go), publish path in [`internal/runmanager/manager.go`](internal/runmanager/manager.go) |
| 3 | gRPC server in cmd/api | ✅ | [`internal/grpcsvc/workers.go`](internal/grpcsvc/workers.go) embeds `teov1.UnimplementedWorkersServer` from [`internal/proto/teov1/`](internal/proto/teov1) (binary protobuf wire format); generation wired via [`proto/buf.yaml`](proto/buf.yaml) + [`proto/buf.gen.yaml`](proto/buf.gen.yaml) and the `make proto` target. JSON codec deleted. **Runs gRPC service** (`CreateRun`/`GetRun`/`CancelRun`) now implemented in [`internal/grpcsvc/runs.go`](internal/grpcsvc/runs.go) (embeds `UnimplementedRunsServer`) over the shared transport-agnostic [`internal/runsvc.Service`](internal/runsvc/service.go) — the same code path HTTP/GraphQL uses for run intake. Registered in [`cmd/api/main.go`](cmd/api/main.go) via `grpcsvc.RegisterRuns` next to `WorkersService`; domain errors map to gRPC codes (validation→`InvalidArgument`, missing repo/run→`NotFound`, idempotency conflict→`AlreadyExists`, anything else→generic `Internal` with the raw error logged server-side, not leaked on the wire). **Auth enforced**: the gRPC server now installs `grpcsvc.AuthUnaryInterceptor` ([`internal/grpcsvc/auth.go`](internal/grpcsvc/auth.go)) which validates a JWT/API-key from `authorization` metadata using the same primitives as the HTTP middleware and rejects unauthenticated callers of the Runs RPCs with `codes.Unauthenticated` (Workers dispatch RPCs stay open as internal traffic). `runToProto` saturates `int`→`int32` (no millisecond-aggregate overflow wrap); `RegisterRuns` panics at wiring time if `Svc` is nil. Table-driven unit tests cover `grpcErr`/`runToProto`/`protoToCreateReq`/`clampInt32`, the interceptor (gated-reject, valid-JWT-pass, ungated pass-through), and `validateCreateRun`/`ValidationError` in `internal/runsvc`. |
| 4 | Real per-test history query in predictor trainer **and** serve path | ✅ | [`services/predictor-ml/src/teo_predictor_ml/train.py`](services/predictor-ml/src/teo_predictor_ml/train.py), [`services/predictor-ml/src/teo_predictor_ml/repo.py`](services/predictor-ml/src/teo_predictor_ml/repo.py). **History source corrected to Postgres `teo.test_executions`** (the same table the always-on Go heuristic reads, [`internal/predictor/predictor.go`](internal/predictor/predictor.go) `loadStats`), keyed by `path::name`, for BOTH training and serving. The previous serve query hit a non-existent `teo.span_events` column set (and an earlier draft used ClickHouse `teo.test_runs`, which nothing in the repo writes) — every history fetch raised, was swallowed, and the model only ever emitted its bias term. Train/serve feature skew (key + time-feature source) eliminated: commit-diff and time-of-day features are zero in both paths and documented as the canonical definition. `clickhouse-driver` dep + dead `_parse_dsn` removed. ML client now surfaces server-side `used_fallback` 200s via `OnServerColdStart` → new `teo_predictor_server_coldstart_total` counter (MAE-drift signal) and logs it; `Fallback` no longer nil-panics on direct struct construction. Go coverage added: [`internal/predictor/mlclient_test.go`](internal/predictor/mlclient_test.go) (status/decode/length-mismatch/transport/empty-input/server-coldstart paths, **request-body snake_case shape decode, `unexpected status 500` message assertion, and a bounded-deadline slow-server timeout test using a release-channel — no `time.Sleep`**) + [`internal/predictor/fallback_test.go`](internal/predictor/fallback_test.go) (primary-success, error/length-mismatch fallback, OnFallback-once, nil-primary delegate, nil-logger no-panic, **plus a `log/slog`-buffer Warn-capture test, a both-ML-and-heuristic-error case asserting the heuristic's error propagates via `errors.Is`, ML-verbatim-on-success, and a compile-time `Predictor`-interface guard for `*Fallback`/`*MLClient`/`*Heuristic`**). **`cmd/predictor` now has [`cmd/predictor/main_test.go`](cmd/predictor/main_test.go)**: the `/healthz` proxy is extracted into a testable `healthzProxyHandler` and exercised against an httptest upstream (200 mirror / non-200 mirror / unreachable→503), and a no-args smoke test builds + runs `predictor` to assert it prints build identity and does NOT start the listener. **Heuristic integration test** [`internal/predictor/heuristic_integration_test.go`](internal/predictor/heuristic_integration_test.go) (`//go:build integration`, `internal/testpg`) seeds repos/tests/test_executions and asserts non-cold-start for ≥3-attempt tests, cold-start below threshold + unknown-repo — compiles under `go vet -tags=integration`; not run here (Docker unavailable). **Python:** [`services/predictor-ml/tests/test_app.py`](services/predictor-ml/tests/test_app.py) (FastAPI TestClient: model path used_fallback=false/p50≥50, repo_id=None cold-start, model-missing cold-start, history-fetch-raises→{} swallow) + [`services/predictor-ml/tests/test_features.py`](services/predictor-ml/tests/test_features.py) (`from_history`/`_features_for` present-vs-absent, `as_array()` length == COLUMN_NAMES) — **14 pytest cases green**. |
| 5 | GraphQL read API | ✅ | [`internal/api/graphql.go`](internal/api/graphql.go) — endpoint live, schema served at `/graphql/schema`. UI swap landed under E-09 (zero `/api/v1/*` REST calls in `web/src/app/`). **Authz now enforced**: route returns 401 for missing principal, 403 for principals with no role; the `rerunFailed` mutation gates on `RoleEngineer`/`RoleAdmin` via the new `requireMutationRole` helper, so read-only browsers can read but cannot mutate. **Schema extended (additive) for the redesigned marquee UI** (`graphql-schema-fields`): `FailureCluster` now exposes spatial-map coords `x`/`y`/`r` + `category`/`stackFingerprint`/`affectedRuns` (computed server-side in `computeClusterLayout`/`classifyClusterCategory` from `teo.failure_clusters` + a `failure_cluster_id` affected-run subquery — x/y/r are presentation-only, relative to the returned page, not stable IDs); `FlakeRecord` adds `wilsonUpper` + `spark` (last-20 P/F/S outcomes, chronological, one batched query) + `status` + `durationMeanMs`; `Run` adds a `predictor { mae rho modelVersion p50DeltaMs p95DeltaMs sampleCount confidence }` object computed from finished shards (`queryRunPredictor`; `modelVersion` falls back to `heuristic`, returns null for <2 finished shards), and `Shard` adds `deltaMs` (actual-predicted). Per-test predictor confidence stays out of scope (only shard-level predicted/actual is stored). `web/src/lib/queries.ts` fetches the new fields. Unblocked `ui-clusters-flakes` and `ui-home-calibration`, **both of which have since rewired off the `teo-data.ts` MOCK** — `/clusters` + `/flakes` now render from `FailureClustersQuery`/`FlakesQuery` via the `web/src/lib/teo-adapt.ts` adapter (see FR-700) and the `/` home + predictor-calibration overlay render from `RunsQuery`/`RunByIdQuery` via `adaptRun`/`adaptStatus` (see reconciliation row 7). |
| 6 | Lint sweep | ✅ | `slices.Contains`, `strings.CutPrefix`/`CutSuffix`, builtin `max`/`min`, `any` over `interface{}`, dead helpers removed. |

---

## Functional requirement coverage

Each FR from [`docs/requirements/functional.md`](docs/requirements/functional.md) tracked here. ✅ = wired end-to-end; 🟡 = code path exists with a documented follow-up; ⏳ = pending; 📦 = deferred.

> ✅ Reconciliation update (2026-05-20): the audit's functional gaps are all closed — **FR-304** (`teo replay` #1 + S3 plan archive #2), **FR-703–705** (#5 log-tail viewer), **FR-801** (#6 OIDC sign-in), and the Go/Python **AST-signature fingerprints** (#3/#4); a new `teo discover` CLI closes **S-06-01 AC1**. Only polish/test-debt items remain (see the [Backlog reconciliation](#backlog-reconciliation--verified-incomplete-items-2026-05-20) table).

### FR-100 Run intake
- ✅ FR-101..104: create/cancel via REST, idempotency via `Idempotency-Key`
- ✅ FR-105: budget enforcement loop in Run Manager
- ✅ FR-106: `pytest` + `go test` + `jest` adapters present (E-14); runner-image variants wired into the Helm chart at [`deploy/helm/teo/values.yaml`](deploy/helm/teo/values.yaml) (`workers.runners.{pytest,gotest,jest}.image`)

### FR-200 Test discovery & manifest
- ✅ FR-201..204: pytest discovery with `--collect-only`, manifest schema in `internal/model`

### FR-300 Scheduling & sharding
- ✅ FR-301..306: LPT, exclusivity, plan persistence, replay-friendly
- ✅ FR-308: Karpenter NodePools + IMDSv2 poller + Agent draining state machine + Run Manager reschedule sweep + worker reshard manifest lookup; preemption_count visible per run
- 📦 FR-307 (speculative re-execution), 📦 FR-309 (cost budget)

### FR-400 Worker execution
- ✅ FR-401..403: claim, execute, OTel span emission per test
- ✅ FR-404: per-test stdout/stderr captures uploaded to S3 via the new `internal/logstore` package. `Uploader` interface; production `S3` impl wraps `aws-sdk-go-v2/feature/s3/transfermanager` (auto-promotes to multipart >16MB); `Noop` impl for dev/CI. Worker's `recordResult` redacts both streams + the failure block, concatenates them, uploads to `runs/{runID}/shards/{shardID}/tests/{testID}/{attempt}.log`, and persists the resulting key into `test_executions.log_object_key`. `cmd/worker` constructs the S3 uploader when `TEO_S3_BUCKET` is set; otherwise `Noop`. Empty captures skip the upload.
- ✅ FR-405..407: heartbeat, idempotent reports, ShardFinished
- ✅ FR-408: redactor on the worker

### FR-500 Result aggregation
- ✅ FR-501: at-least-once writes + idempotent upsert (`(shard, test, attempt)` unique constraint)
- ✅ FR-502: failure clustering by stack-trace fingerprint
- ✅ FR-503/504: JUnit + OTLP export endpoints at `GET /api/v1/runs/{id}/export?format={junit|otlp}`. JUnit XML maps outcomes (passed→empty, failed→`<failure>`, errored/timed_out/interrupted→`<error>`, skipped→`<skipped/>`), embeds the failure-cluster representative message + stack on failed cases, and emits per-suite + total roll-ups. OTLP rebuilds `collectorpb.ExportTraceServiceRequest` from `teo.span_events` and ships binary proto by default (`?as=json` for `protojson`). When ClickHouse isn't configured, OTLP returns 501; JUnit still works because it reads Postgres only. The `SpanQuerier` interface keeps the OTLP path unit-testable.
- ✅ FR-505: rerun-failed mutation — see FR-700 for the UI side; the GraphQL mutation `rerunFailed(runId)` resolves through `internal/api/graphql_resolvers.go` and is covered by both unit and integration tests.

### FR-600 Flake detection
- ✅ FR-601..604: Wilson interval, classification, nightly job
- ✅ FR-605..606: auto-quarantine state transition, non-blocking lane
- ✅ FR-607: ML predictor service with heuristic fallback — now wired end-to-end. Go `internal/predictor.MLClient` POSTs to `<TEO_PREDICTOR_ML_URL>/v1/predict`; `internal/predictor.Fallback` tries ML first and transparently reverts to the Heuristic on timeout/connection-error/non-200/decode-error/length-mismatch (incrementing `teo_predictor_fallback_total` via an `OnFallback` hook wired in cmd/run-manager, logged via slog). The Run Manager builds the Fallback only when `TEO_PREDICTOR_ML_URL` is set (heuristic-only otherwise; nothing hard-fails if the Python service is down). Python `_resolve_repo_id` now queries Postgres (`teo.repos`, TTL-cached) and `_features_for` builds real feature vectors from a prefetched per-repo ClickHouse 30-day history (`repo.py`); cold-start stays a genuine fallback (no DSN / unknown repo / model miss / DB error). `cmd/predictor` repurposed to a `/healthz` proxy gated on `TEO_PREDICTOR_ML_URL` (no-args build-identity smoke path preserved). Note: HTTP (not the ADR-0019 gRPC) is used deliberately — documented divergence.
- ✅ FR-609: GitHub Issue creation, dedupe-via-comment, CODEOWNERS-resolved assignees, SLA nudge sweeper, un-quarantine proposal flow with magic-link restore endpoint

### FR-700 UI
- ✅ FR-701..705: run list, run timeline, failure clusters, flake history — all on GraphQL via `gqlFetch` + named operations from `web/src/lib/queries.ts`. The redesigned `/clusters` + `/flakes` screens (`web/src/components/teo/{Clusters,Flakes}.tsx`) are now GraphQL-backed too: both page server components fetch `FailureClustersQuery`/`FlakesQuery` and map rows through the pure `web/src/lib/teo-adapt.ts` adapter (no longer the `TEO_DATA` mock). Design-only fields with no DB source (cluster `tests[]`/`related[]`, flake `owner`/`quarantinedDays`) are derived deterministically in the adapter; both screens render an empty state on zero rows / API outage. **Hardened (`ui-clusters-flakes` review sweep):** fixed a Rules-of-Hooks defect in `ClustersScreen` (the empty-state early return sat between two `useMemo` calls, so an in-place empty↔non-empty prop flip would throw "Rendered fewer/more hooks"; the return now lives below all hooks); `gqlFetch` now logs non-2xx and GraphQL-level errors server-side so an outage is observable rather than silently masked as no-data; the `/clusters` + `/flakes` page server components wrap the fetch in try/catch so a hard `fetch` reject (DNS/connection-refused/unset `TEO_API_URL`) degrades to the empty state instead of throwing the route, and the comments now describe the actual resilience. The previously-untested `teo-adapt.ts` adapter gained `web/src/lib/teo-adapt.test.ts` (40 vitest cases incl. queries.test) covering `hash32`/`classifyCategory`/`extractFile`/`deriveOwner`/`deriveSpark`/`daysSince` + `adaptClusters`/`adaptFlakes` passthrough-vs-fallback; `classifyCategory` was reconciled to mirror Go `classifyClusterCategory` exactly (panic→timeout→network→race precedence, same substrings); and `queries.test.ts` now asserts the 1-char spatial-map fields (`x`/`y`/`r`) as selection tokens anchored to whitespace instead of trivially-passing substring matches. **Quarantine + owner wiring (ui-clusters-flakes test sweep):** `FlakeRecord` gained two additive fields — `quarantinedAt` (RFC3339, null when not quarantined; `queryFlakes` `COALESCE(fr.quarantined_at, t.quarantined_at)`) and `ownerTeam` (`teo.tests.owner_team`) — surfaced in `graphql.go`/`server.go` SDL/`queries.ts`. `adaptFlakes` now resolves `status='quarantined'` + `quarantinedDays>0` from `quarantinedAt`, `'flagged'` when `wilsonLower>0.05` and not quarantined, and seeds the owner avatar from `ownerTeam` when present (else a testId hash). The redesigned screens are prop-driven and tested directly: `web/src/components/teo/{Clusters,Flakes}.test.tsx` (@testing-library/react — list/detail selection, count + Tracked KPI, empty-state no-throw / no-NaN; a `ResizeObserver` stub was added to `web/src/test-setup.ts` so the spatial map mounts under jsdom). Go side: `graphql_schema_test.go` asserts the two new `FlakeRecord` fields + a `Do()` stub-source resolve; `graphql_integration_test.go` seeds `owner_team`/`quarantined_at` and round-trips `wilson_upper`/`status`/`quarantined_at`/`owner_team` while re-checking the `wilson_lower>0.05` gate and that `representativeStack` resolves as a single string; the `graphql_http_integration_test.go` roundtrip selects the new fields end-to-end (integration tiers compile via `go vet -tags=integration`, not run where Docker is unavailable).
- ✅ FR-706: live updates via 2s polling on the run-detail page (`LiveRunShards`); polling auto-stops on terminal status; tested with fake timers
- ✅ FR-505 (rerun-failed): UI button + Next API route + GraphQL mutation; navigates to the new run on success
- ✅ FR-708: weekly per-author digest with WoW delta, SMTP + Slack delivery, opt-out enforcement, dry-run CLI
- ✅ FR-709 (cost dashboard): weekly $/build trend + spot share at `/cost`. New `internal/cost` pricing helper (Pricer with SpotPerMin + OnDemandPerMin rates; reads `TEO_COST_SPOT_PER_MIN` / `TEO_COST_ONDEMAND_PER_MIN` overrides; defaults $0.012/$0.040 per worker-minute). New `costSummary(weeks: Int = 8)` GraphQL query aggregates `teo.runs` rows by `date_trunc('week', started_at)` and emits `CostWeek { weekStart, runs, spotMinutes, onDemandMinutes, totalCost, costPerBuild, spotShare }`. Next.js page server-renders the table + a CSS bar visualization of the per-build trend (no charting lib).

### FR-800 Identity & authz
- ✅ FR-801: OIDC via Dex (chart wiring); backend `/auth/*` + httpOnly `teo_session` cookie; **frontend wired 2026-06-10** (`web/src/middleware.ts` soft sign-in gate behind `NEXT_PUBLIC_REQUIRE_AUTH`, `SessionNav` mounted in a non-avatar `topbar__session` container with proactive `/auth/refresh`). **Review fixes 2026-06-10:** dropped the dead `?next=` redirect param (no end-to-end return-to plumbing — post-sign-in landing is owned by the Go OIDC callback); `SessionNav` treats `NEXT_PUBLIC_SESSION_REFRESH_MS<=0` as "disable refresh" and clears the interval on a failed refresh; tightened the middleware matcher to anchor `login`/`favicon.ico` as exact segments; added `middleware.test.ts` + `SessionNav` refresh-path fake-timer tests. **Test hardening 2026-06-10:** added a defense-in-depth in-function allow-list guard in `middleware.ts` (never gates `/login`, `/auth/*`, `/api/*` even if invoked directly) and extended `middleware.test.ts` to assert at the invocation level that `/login`, `/auth/callback`, and `/api/graphql/run` are never redirected with the gate on, plus the `NextRequest` + `cookies.set('teo_session', …)` redirect/pass-through pair (15 middleware + 9 SessionNav cases; full `web` suite 142 green)
- ✅ FR-802: API keys with prefix + argon2id hash
- ✅ FR-803..805: roles, audit logging, revocation within 30s

### FR-900 GitHub integration
- ✅ FR-901: GitHub App scaffold; webhook signature verification
- ✅ FR-902/903: `RunObserver` pattern in Run Manager; `CheckObserver` creates/updates/finalizes the Check Run; failure summary embeds the top-3 failure clusters (Markdown), deep-link to TEO via `TEO_BASE_URL`
- ✅ FR-904: HMAC verification + 401 on tamper

### FR-1000 Operations
- ✅ FR-1001: Helm install path (`helm install teo deploy/helm/teo …`); subcharts vendored in `Chart.yaml`; chart-testing CI job templates the on/off matrix
- ✅ FR-1002: pre-upgrade migration Job
- ✅ FR-1003: `/healthz`, `/readyz` on API; **`internal/metrics`** centralises every `teo_*` collector (the dashboards/alerts shipped in E-11 reference these names exactly); chi middleware records `http_server_requests_seconds` per (handler, method, status) with the chi RoutePattern as label so cardinality stays bounded; Run Manager emits `teo_runs_active`, `teo_run_transitions_total`, `teo_runs_stuck_total`, `teo_scheduler_plan_seconds`/`_total`; result-pipeline emits `teo_clickhouse_inserts_total` + `teo_clickhouse_insert_seconds` + `_failures_total`; `/metrics` served on the API + dedicated `:9100` listener on the headless services.
- ✅ FR-1004: CloudNativePG + clickhouse-backup operator vendored; restore drill runbook at `docs/operations/restore-drill.md`
- ✅ FR-1005: `teo doctor` CLI runs Postgres / ClickHouse / NATS / API / predictor checks in parallel under a single deadline. Skipped checks (no DSN/URL configured) don't fail the exit code; any Fail flips it to 1. `--json` flag for scripting; humans get a tabwriter-aligned table with a one-line summary. The `internal/doctor` package is the reusable backbone — same Check interface usable from a future in-process diagnostic endpoint.

### Deferred (📦 confirmed via ADR-0012 revised)
- 📦 What-if simulator
- 📦 Cost-budgeted execution
- 📦 LLM root-cause hints
- 📦 RSpec / JUnit-direct / Bazel adapters
- 📦 Multi-cloud worker pool
- 📦 Speculative re-execution
- 📦 Cross-repo test impact analysis
- 📦 SOC 2 readiness

---

## Test coverage snapshot

```
ok  internal/api                       (graphql schema + resolvers; OTLP export
                                        round-trip via stub SpanQuerier; hexToBytes
                                        + formatSeconds helpers)
ok  internal/audit                     (nil-Logger + nil-pool no-op guards on Log)
ok  internal/cost                      (Pricer.RunCost on positive/zero/negative
                                        inputs; NewFromEnv default + override +
                                        invalid/negative-fallback paths)
ok  internal/db                        (parseClickHouseDSN: full URL, default db,
                                        no-creds, user-only, applied defaults,
                                        malformed-URL rejection)
ok  internal/logstore                  (Noop body-drain + nil-safe; S3 conforms to
                                        Uploader interface)
ok  internal/predictor                 (NewHeuristic seeded defaults; Predict nil
                                        guards; coldStart fingerprint+P95 = 3×P50;
                                        coldOnly preserves order; defaultFor lookup
                                        + fallback)
ok  internal/quarantine                (issue-body markdown structure + key-fact
                                        substitution + zero-sample NaN guard;
                                        GitHubOpener nil-receiver + nil-client
                                        Open/Comment guards)
ok  internal/worker                    (drain idempotency; uploadLog key shape +
                                        redaction + skip-when-empty + upload-error
                                        handling)
ok  internal/auth                      (JWT roundtrip, argon2id verify+tamper)
ok  internal/codeowners                (rule precedence, path matching)
ok  internal/digest                    (SMTP MIME composition, ctx cancel, Slack
                                        webhook payload + 4xx fail, Multiplex fanout,
                                        owner→user matching)
ok  internal/flake                     (Wilson textbook, classify states)
ok  internal/github                    (HMAC verify valid + tampered + malformed)
ok  internal/migrate                   (statement splitting)
ok  internal/nats                      (Connect("") → ErrUnavailable; ShardDispatch
                                        JSON wire format incl. time.Time round-trip
                                        + omitempty optional fields; subject/stream
                                        constants pinned)
ok  internal/redact                    (AWS keys, JWTs, PATs scrubbed; clean text untouched)
ok  internal/resultpipeline            (Python fingerprint stable across line-numbers; OTLP attr/status mapping)
ok  internal/runmanager                (state transitions, terminal detection)
ok  internal/scheduler                 (LPT determinism, exclusivity, quarantine,
                                        makespan ratio ≤ 4/3 × brute-force optimum on 30 instances)
ok  internal/spot                      (IMDS interruption parsing)
ok  internal/version                   (build identity round-trip)
ok  pkg/adapter/gotest                 (dedupe order/empty; mergeEnv append/no-op;
                                        processEvents pass/fail/skip + package-level
                                        ignore + malformed-line skip + synthetic
                                        entry; New defaults)
ok  pkg/adapter/jest                   (translate full status table; mergeEnv
                                        append/no-op; parseListTests path-relative
                                        + malformed-rejection; parseReport assertion
                                        emit + nested ancestors + todo→skipped +
                                        empty-no-op + malformed-rejection)
ok  pkg/adapter/pytest                 (collect-only parser)
ok  pkg/adapter/template               (SPI invariants: Name non-empty, empty-tests
                                        no-op, unknown-status → errored, mergeEnv)
```

**Now covered by integration tests** (`-tags=integration`): `internal/api/graphql_resolvers.go` (every read resolver + the rerunFailed mutation, against a real Postgres), full `/graphql` HTTP roundtrip via `api.Server`, **run-export REST endpoints** (JUnit XML happy-path + mixed-outcome shape, OTLP+JSON happy-path with stubbed SpanQuerier, 400/404/501 negative paths), **run-intake REST endpoints** (`internal/api/runs.go` Create/Get/Cancel — happy paths, validation, idempotency-key replay returns same id, auth-required 401, unknown-repo 404, terminal-run cancel idempotent), **Runs gRPC service** (`internal/grpcsvc/runs.go` CreateRun/GetRun/CancelRun over a bufconn server with the production `AuthUnaryInterceptor`, against a real Postgres — pending-run create writes one `teo.runs` + one `teo.run_plans` row, unknown-repo→`NotFound`, empty-manifest→`InvalidArgument`, missing-auth→`Unauthenticated`, idempotency-key replay returns same id with one row, different-commit reuse→`AlreadyExists`, GetRun round-trip + random-uuid `NotFound`, CancelRun running→`cancelled` / succeeded idempotent / missing→`NotFound`; mirrors `api.TestPOSTRunsCreatesRow` so HTTP and gRPC are proven to share the `runsvc` path without drift), **logstore S3 round-trip against MinIO** (small body via single PUT, >16MB body promoted to multipart by transfermanager, same-key overwrite — uses a dedicated `internal/testminio.Start` testcontainer harness). **`internal/nats`** is now unit-tested for the broker-free surface: empty-URL `Connect` returns `ErrUnavailable`, `ShardDispatch` JSON wire format pinned (field tags + `time.Time` round-trip + `omitempty` on optional `DispatchTest` fields), and the five subject/stream constants are pinned against accidental rename. The `Connect`/`EnsureStreams`/`Publish` paths that need a live broker remain integration-tier. **Still uncovered (all leaf packages):** `internal/config` (struct definitions only), `internal/model` (data types). (`internal/grpcsvc` now has unit tests for `grpcErr`/`runToProto`/`protoToCreateReq`/`clampInt32`/the auth interceptor, plus the bufconn+testpg Runs integration suite above.) The DB-backed code paths in `internal/audit`, `internal/db`, `internal/predictor`, `internal/quarantine` daemons remain integration-tier — the harnesses in `internal/testpg/` and `internal/testminio/` make those follow-ups straightforward.

---

## What's next (named follow-ups, prioritized)

The core of all 16 epics is wired, but the 2026-05-20 audit reopened **12 backlog items** (see [Backlog reconciliation](#backlog-reconciliation--verified-incomplete-items-2026-05-20)). Prioritized below; close or formally defer (ADR + row update) each before tagging v1.0.0.

**Resolved 2026-05-20:** #1 `teo replay` CLI, #2 S3 plan archive, #3 Go AST fingerprint, #4 Python AST fingerprint, #5 log-tail viewer, #6 OIDC sign-in flow (see the reconciliation table). Also added `teo discover` (closes S-06-01 AC1). **Every functional gap from the audit is now closed.**

**Remaining — none.** All 12 reconciliation items are closed. (#7 calibration overlay ✅ 2026-06-10; #8 Mermaid issue history ✅ 2026-06-10; #9 failure-cluster backfill ✅ 2026-06-10; #10 ClickHouse load test ✅ 2026-06-10; #11 run-list virtual scroll ✅ 2026-06-10; #12 leader-election chaos/integration test ✅ 2026-06-10 — executed green against real Postgres.)

**Lower priority (polish / coverage):** all closed. (Failure-cluster backfill #9 / T-07-02-03 — ✅ done 2026-06-10; Mermaid issue history #8 / S-15-02 — ✅ done 2026-06-10; predicted-vs-observed calibration overlay #7 / S-12-04 — ✅ done 2026-06-10; run-list virtual scroll #11 / S-09-01 — ✅ done 2026-06-10.)

**Failure-cluster backfill test hardening (2026-06-10):** added the spec-mandated unit suite in `internal/resultpipeline/backfill_test.go` (assigns-cluster w/ live-fingerprint-reuse assertion, skips-no-stack, dry-run-no-mutate w/ distinct-cluster preview, continues-on-per-row-error, empty-fingerprint-skip, since-zero-means-all) plus the `//go:build integration` `internal/resultpipeline/backfill_integration_test.go` (end-to-end back-link + idempotency, ignores-passed-and-already-assigned). Added `Stats.PreviewClusters` (distinct-fingerprint count, populated in dry-run only) to back the preview assertion. Integration tests compile + `go vet -tags=integration` clean but were NOT executed (Docker unavailable on the dev host).

**ClickHouse load-test harness (2026-06-10):** added `internal/testch` (testcontainers ClickHouse harness, `/ping`-gated, `migrate.Up` + `OpenClickHouseConn`) and `internal/resultpipeline/otlp_loadtest_integration_test.go` (`//go:build integration`, `TestClickHouseSpanInsertLoad` + `TestClickHouseHarnessSmoke`) driving the production `writeSpans` path at ~1M rows (env-tunable, 10k under `-short`) with throughput + p50/p95/p99 via `percentile` and an exact-count assertion. Pure logic in no-tag `internal/resultpipeline/loadmetrics.go`. Run command + not-yet-run p99 table in `docs/operations/clickhouse-load-test.md`. Compiles + `go vet -tags=integration` clean; NOT executed (Docker unavailable) — p99 numbers deliberately left `TODO` (no fabrication), to be captured on first CI testcontainers run.

**Test/verification debt:**
6. Leader-election 2-replica chaos test (#12, S-04-02) — ✅ done 2026-06-10; `leader_election_integration_test.go` written and **executed green against real Postgres via testcontainers** (lock-held-backoff + takeover, and 4-replica concurrent convergence). (#10 ClickHouse 1M-row load test ✅ 2026-06-10 — harness written + vet-clean, still awaiting a CI run to record p99.)

Already real and not in question: build is clean; 25 unit packages + 38 integration tests green; `make proto` → `buf generate` wiring landed (binary-protobuf gRPC codec); the release pipeline (E-11) cuts a tag → goreleaser → cosign-signed binaries + SBOMs + chart-released Helm chart; restore-drill runbook written; metrics + dashboards + alerts emit and fire.

---

## How this file stays accurate

- Every PR that lands an FR or closes a follow-up updates the corresponding row here in the same commit.
- Reviewers refuse to merge a PR that changes behavior without a `progress.md` delta.
- The CI pipeline does not enforce this yet — it's a social contract until a `progress-lint` step lands.
